🎯 **What:** Simplified the summary dictionary construction logic in `cmd_momentum_features` within `bist_signal_bot/cli/commands.py`.
💡 **Why:** Reduced cyclomatic complexity from D (26) by replacing deep sequential `if` checks with dictionary comprehension and a static keys list. This significantly enhances maintainability and code readability while preserving the same logic.
✅ **Verification:** Verified by checking `git diff` and executing `poetry run pytest bist_signal_bot/tests/test_cli_momentum_indicators.py` to ensure feature calculation logic tests pass with the new refactored loop format.
✨ **Result:** A more readable, maintainable control flow avoiding deep branching.
