1. Add `python test_quality.py` to ensure logic works and that the calendar provider is actually working properly since the reviewer had doubts. Note: the `calendar.py` file already exists in `bist_signal_bot/markets/calendar.py` as seen by my logs.
2. Complete memory recording.
