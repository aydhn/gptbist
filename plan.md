1. **Prepare Python script `generate_phase101.py`**: Create this script to automate the generation and modification of Phase 101 files, including `performance/models.py`, `performance/timers.py`, `performance/profiler.py`, `performance/resource_budget.py`, `performance/cache.py`, `performance/benchmark.py`, `performance/bottlenecks.py`, `performance/regression.py`, `performance/storage.py`, `performance/reporting.py`, `app/performance_app.py`, and 15 pytest files.
2. **Modify `core/exceptions.py`**: Add `PerformanceError`, `PerformanceTimerError`, `PerformanceProfilerError`, `ResourceBudgetError`, `LocalCacheError`, `PerformanceBenchmarkError`, `BottleneckAnalysisError`, `PerformanceRegressionError`, `PerformanceStorageError` to `bist_signal_bot/core/exceptions.py`. Verify with `cat bist_signal_bot/core/exceptions.py | grep Performance`.
3. **Modify `config/settings.py`**: Add performance-related settings (e.g. `ENABLE_PERFORMANCE`, `PERFORMANCE_DIR_NAME`, `PERFORMANCE_DEFAULT_MAX_RUNTIME_SECONDS`, etc) and update `.env.example`. Verify with `cat bist_signal_bot/config/settings.py | grep ENABLE_PERFORMANCE`.
4. **Modify `core/audit.py`**: Add event types like `PERFORMANCE_PROFILE_CREATED`, `RESOURCE_BUDGET_EVALUATED`, `CACHE_ENTRY_CREATED`, etc. to `AuditEventType`. Verify with `grep CACHE_ENTRY_CREATED bist_signal_bot/core/audit.py`.
5. **Modify `storage/paths.py`**: Add `get_performance_dir` method to return `DATA_DIR / settings.PERFORMANCE_DIR_NAME` or similar. Verify with `grep get_performance_dir bist_signal_bot/storage/paths.py`.
6. **Modify `cli/commands.py` & `cli/parsers.py`**: Add the `performance` CLI command with subcommands `profile`, `benchmark`, `budgets`, `cache`, `bottlenecks`, `regressions`, `report`, `recent`, `config`. Verify changes via `grep add_performance_parser bist_signal_bot/cli/parsers.py`.
7. **Integrate `--performance` flags**: Update `bist_signal_bot/app/healthcheck.py`, `bist_signal_bot/maintenance/doctor.py`, `bist_signal_bot/qa/release_gate.py`, `bist_signal_bot/ops/readiness.py`, and `bist_signal_bot/reports/collector.py` to accept and process the new performance flags.
8. **Update `notifications/formatter.py`**: Add functions to format performance outputs like `format_performance_profile`, `format_benchmark_result`, `format_bottleneck_findings`, `format_performance_regressions`, and `format_performance_report`.
9. **Update Documentation**: Add section to `README.md`. Create `bist_signal_bot/docs/81_LOCAL_PERFORMANCE_OPTIMIZATION.md` and `bist_signal_bot/examples/performance_workflow.md` describing performance capabilities.
10. **Pre-commit**: Complete pre-commit steps to make sure proper testing, verifications, reviews and reflections are done.
11. **Verify & Execute**: Run `python3 generate_phase101.py`, run `pytest` to ensure all tests pass and test the added CLI commands manually.
