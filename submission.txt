Finished optimization of load_latest_composed_report and load_composed_reports in bist_signal_bot/report_templates/storage.py. Code now uses `f.readlines()` to loop lines backwards, thus optimizing the number of iterations and the JSON parsing / Pydantic object creation dynamically, since it exits early as soon as the criteria is met (`limit` reached or `kind` found).

Tested locally with benchmark indicating execution dropping from ~5.3 seconds to ~0.17 seconds (x30 performance improvements when reading up to 5000 items). Tested with `pytest` locally which passed.

Ready for submission.
