🎯 **What:** The `get_default_env_file` function in `bist_signal_bot/config/env_loader.py` was previously untested.
📊 **Coverage:** Added a test case `test_get_default_env_file` that verifies the function returns a `Path` object pointing to the `.env` file at the root of the project structure. It accounts for execution across different typical CI or local folder environments (e.g. `bist-signal-bot`, `workspace`).
✨ **Result:** Improved test coverage and ensured reliability of the environment file loading mechanism.
