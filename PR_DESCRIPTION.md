🎯 **What:** Added a missing test coverage for `create_scan_output_dir` in `storage.py` by covering edge cases such as empty `strategy_name`.
📊 **Coverage:** A test for empty `strategy_name` in `ScanRequest` that correctly formats the sub-directory paths and validates its creation without errors.
✨ **Result:** 100% test coverage maintained and enhanced for edge cases, reducing future regression risks.
